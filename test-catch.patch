--- apps/extension/src/ChatViewProvider.ts
+++ apps/extension/src/ChatViewProvider.ts
@@ -109,14 +109,19 @@
 
     // Call Hermes API
     let fullContent = '';
-    await this._hermesClient.streamChat(text, contextString, (chunk: string) => {
-      fullContent += chunk;
-      this.postMessage({
-        type: 'updateMessage',
-        messageId: responseId,
-        content: fullContent,
+    try {
+      await this._hermesClient.streamChat(text, contextString, (chunk: string) => {
+        fullContent += chunk;
+        this.postMessage({
+          type: 'updateMessage',
+          messageId: responseId,
+          content: fullContent,
+        });
       });
-    });
+    } catch (e: any) {
+      console.error(e);
+      fullContent += `\\n\\nError: ${e.message}`;
+    }
 
     // Finalize message status
     this.postMessage({
